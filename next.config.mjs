/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.imgflip.com'], // Add allowed image domains here
      },
};
// next.config.mjs

  

export default nextConfig;
